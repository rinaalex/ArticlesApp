<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ArticlesAppMobile.UI.Pages.Accounts.RegisterPage"
             Title="Registration page">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Login"/>
            <Entry Text="{Binding LoginObject.Login}"/>
            <Label Text="Password"/>
            <Entry Text="{Binding LoginObject.Password}"/>
            <Button Text="Submit" Command="{Binding SubmitCommand}"/>
            <Label Text="{Binding LastError}" TextColor="Red" IsEnabled="{Binding HasError}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>