<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ArticlesAppMobile.UI.Pages.Accounts.LoginPage"
             Title="Login page">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Username"/>
            <Entry Text="{Binding LoginModel.Login}"/>
            <Label Text="Password"/>
            <Entry Text="{Binding LoginModel.Password}" IsPassword="True"/>
            <Button Text="Login" Command="{Binding SubmitCommand}"/>
            <Button Text="Registration" Command="{Binding RegistrationCommand}"/>
            <Label Text="{Binding LastError}" IsEnabled="{Binding HasError}" TextColor="Red"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>