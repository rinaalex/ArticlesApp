<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ArticlesAppMobile.UI.Pages.Articles.ArticlePage"
             Title="Article page">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Article.Title}"/>
            <Entry Text="{Binding Article.Content}" IsReadOnly="True"/>
            <Label Text="{Binding Article.AuthorName}"/>
            <Label Text="{Binding Article.PublicationDate}"/>
            <Label Text="{Binding Article.Raiting}"/>
            <Button Text="Edit article" Command="{Binding EditArticleCommand}"/>
            <Button Text="Add review" Command="{Binding AddReviewCommand}"/>
            <Button Text="Show reviews" Command="{Binding ShowReviewsCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>