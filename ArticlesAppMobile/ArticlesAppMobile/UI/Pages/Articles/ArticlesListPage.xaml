<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ArticlesAppMobile.UI.Pages.Articles.ArticlesListPage"
             Title="Articles page">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Add article" Command="{Binding AddArticleCommand}"/>
            <Label Text="{Binding LastError}" IsEnabled="{Binding HasError}"/>
            <ListView ItemsSource="{Binding ArticlesList}" SelectedItem="{Binding SelectedArticle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}" Detail="{Binding AuthorName}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>