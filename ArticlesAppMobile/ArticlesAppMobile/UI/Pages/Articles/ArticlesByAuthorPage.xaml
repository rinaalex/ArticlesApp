<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ArticlesAppMobile.UI.Pages.Articles.ArticlesByAuthorPage"
             Title="Articles by author">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding ArticlesList}" SelectedItem="{Binding Article}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}" Detail="{Binding Raiting}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label Text="{Binding LastError}" IsEnabled="{Binding HasError}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>