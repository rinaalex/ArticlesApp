<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ArticlesAppMobile.UI.Pages.Reviews.ReviewPage"
             Title="Review page">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Review.AuthorName}"/>
            <Entry Text="{Binding Review.Content}" IsReadOnly="True"/>
            <Label Text="{Binding Review.NumStars}"/>
            <Label Text="{Binding Review.PublicationDate}"/>
            <Button Text="Edit review" Command="{Binding EditReviewCommand}"/>
            <Label Text="{Binding LastError}" IsEnabled="{Binding HasError}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>